import 'package:flutter/material.dart';

class AppColors {
  // Primary — deep blue (legal/professional)
  static const primary = Color(0xFF1A365D);
  static const primaryLight = Color(0xFF2B6CB0);
  static const primaryDark = Color(0xFF0F2440);

  // Accent — gold
  static const accent = Color(0xFFD69E2E);
  static const accentLight = Color(0xFFECC94B);

  // Surface
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF7FAFC);
  static const background = Color(0xFFF0F4F8);

  // Dark mode
  static const darkSurface = Color(0xFF1A202C);
  static const darkSurfaceVariant = Color(0xFF2D3748);
  static const darkBackground = Color(0xFF171923);

  // Text
  static const textPrimary = Color(0xFF1A202C);
  static const textSecondary = Color(0xFF718096);
  static const textOnPrimary = Color(0xFFFFFFFF);
  static const darkTextPrimary = Color(0xFFE2E8F0);
  static const darkTextSecondary = Color(0xFFA0AEC0);

  // Semantic
  static const success = Color(0xFF38A169);
  static const warning = Color(0xFFD69E2E);
  static const error = Color(0xFFE53E3E);
  static const info = Color(0xFF3182CE);

  // Chat
  static const userBubble = Color(0xFF2B6CB0);
  static const assistantBubble = Color(0xFFEDF2F7);
  static const darkUserBubble = Color(0xFF2C5282);
  static const darkAssistantBubble = Color(0xFF2D3748);
}
