% Three-layer workflow memory architecture with dual-mode retrieval
\begin{figure}[t]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[
    >=stealth,
    layer/.style={draw, rounded corners=3pt, minimum width=5.2cm,
                  minimum height=1.3cm, align=center, font=\small},
    service/.style={draw, rounded corners=3pt, minimum width=3.4cm,
                    minimum height=1.0cm, align=center, font=\small},
    agent/.style={draw, dashed, rounded corners=3pt, minimum width=2.8cm,
                  minimum height=0.85cm, align=center, font=\small},
    pullpath/.style={->, thick, color=MidnightBlue},
    pushpath/.style={->, thick, color=BrickRed, dashed},
    feedpath/.style={->, thick, color=OliveGreen, densely dashed},
    labelbox/.style={font=\scriptsize\sffamily, fill=white, inner sep=1pt},
  ]

  % === Layers (right column, x=7) ===
  \node[layer, fill=blue!8] (domain) at (7, 0)
    {\textbf{Domain Layer}\\Laws, court decisions, regulations\\[-1pt]{\scriptsize Qdrant (production)}};

  \node[layer, fill=green!8] (workflow) at (7, -2.4)
    {\textbf{Workflow Layer}\\Principle ledger, ADRs, tool contracts,\\[-1pt]
     validator definitions, git history, issues\\[-1pt]
     {\scriptsize PostgreSQL + Qdrant}};

  \node[layer, fill=orange!8] (practitioner) at (7, -5.0)
    {\textbf{Practitioner Layer}\\Edit-trace summaries, decision patterns,\\[-1pt]
     disambiguation labels, accept/reject history\\[-1pt]
     {\scriptsize rlhf-signals extension}};

  % === Left column (x=0) ===
  \node[agent, fill=blue!5] (session) at (0, 0.2)
    {\textbf{Claude Code Session}\\task description};

  \node[service, fill=gray!10] (memservice) at (0, -2.4)
    {\textbf{Memory Service}\\embed $\to$ retrieve $\to$ re-rank\\[-1pt]$\to$ assemble digest};

  \node[agent, fill=red!5] (orchestrator) at (0, -5.0)
    {\textbf{Long-Term Orchestrator}\\scheduled refresh};

  % === Pull path (blue solid): Session -> MemService -> Layers ===
  \draw[pullpath] (session.south) -- node[labelbox, left, xshift=-2pt]
    {\color{MidnightBlue}\textsc{pull}} (memservice.north);

  % Pull arrows to layers -- use a junction point to fan out cleanly
  \coordinate (pulljunc) at (3.5, -2.4);
  \draw[pullpath] (memservice.east) -- (pulljunc);
  \draw[pullpath] (pulljunc) |- ([yshift=2pt]domain.west);
  \draw[pullpath] (pulljunc) -- (workflow.west);
  \draw[pullpath] (pulljunc) |- ([yshift=-2pt]practitioner.west);

  % Return digest path
  \draw[pullpath, dotted] ([xshift=-6pt]memservice.north) -- ++(0,0.2)
    -- ++(-1.0,0) |- node[labelbox, left, xshift=-2pt]
    {\color{MidnightBlue}digest} ([xshift=-6pt]session.south);

  % === Push path (red dashed): Orchestrator -> MemService -> Layers ===
  \draw[pushpath] (orchestrator.north) -- node[labelbox, left, xshift=-2pt]
    {\color{BrickRed}\textsc{push}} (memservice.south);

  % Push arrows -- offset slightly below pull junction
  \coordinate (pushjunc) at (3.2, -2.7);
  \draw[pushpath] ([yshift=-3pt]memservice.east) -- (pushjunc);
  \draw[pushpath] (pushjunc) |- ([yshift=-4pt]workflow.west);
  \draw[pushpath] (pushjunc) |- ([yshift=-4pt]practitioner.west);

  % === Issue tracker feed ===
  \node[font=\scriptsize\sffamily, below=0.3cm of orchestrator, text=gray]
    (tracker) {Plane issues $\cdot$ git activity};
  \draw[->, thin, gray] (tracker.north) -- (orchestrator.south);

  % === Retrieval-correction feedback loop (green) ===
  \draw[feedpath]
    ([xshift=6pt]practitioner.south) -- ++(0,-0.8)
    -| ([xshift=6pt]memservice.south);
  \node[font=\scriptsize\sffamily, text=OliveGreen, fill=white, inner sep=2pt]
    at (3.5, -6.25) {retrieval-correction feedback};

  % === Legend ===
  \node[font=\scriptsize, inner sep=0pt] at (3.5, -7.2)
  {%
    \tikz[baseline=-0.5ex]\draw[pullpath] (0,0) -- (0.6,0);~\textsc{pull} (session-start hook) \qquad
    \tikz[baseline=-0.5ex]\draw[pushpath] (0,0) -- (0.6,0);~\textsc{push} (scheduled refresh) \qquad
    \tikz[baseline=-0.5ex]\draw[feedpath] (0,0) -- (0.6,0);~feedback loop%
  };

\end{tikzpicture}%
}% end resizebox
\caption{Three-layer workflow memory architecture with dual-mode retrieval.
  \textsc{Pull} mode (solid blue) fires at session start: the task description is embedded
  and used to query all three Qdrant collections in parallel; results are re-ranked and
  assembled into a compact digest.
  \textsc{Push} mode (dashed red) runs on a schedule proportional to task activity,
  refreshing workflow and practitioner layer entries so that pull queries return current context.
  The retrieval-correction feedback loop (green) detects memory entries that \emph{should}
  have been retrieved but were not, feeding back into retrieval tuning and the RLHF
  pipeline as a process-level oversight signal.}
\label{fig:architecture}
\end{figure}
